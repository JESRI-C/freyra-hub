# GoFreyra run-log

Én kompakt post pr. manuel eller planlagt cyklus. Resultater må kun opgraderes efter observeret sluttilstand.

## 2026-08-27 til 2026-08-29 - manuel cyklus 001

- **Udgangspunkt:** `JESRI-C/freyra-hub`, branch `codex/gofreyra-p0`, HEAD `03dca07` ved start. Ingen `AGENTS.md` fundet. Eksisterende ændringer er bevaret.
- **Sandhed etableret:** Package scripts, runtime, auth/Supabase-klienter, offentlige serverruter, migrations/RLS, projektkort/GeoJSON, projektmedier, Før/Efter-komponent, målinger, kildeadaptere, rapportkode og tests er inspiceret statisk.
- **Vigtigste fund:** Publishable nøgle beskytter service-role-ruter; natur-serverfunktion mangler verificeret auth/tenantkontrol; `project_members` kan self-inserte uden rollebegrænsning; migrationshistorikken rummer åbne policies; `.env` var tracket; live Supabase kunne ikke inspiceres; MARS, versionsfast Før/Efter og P0-rapport har dokumenterede huller.
- **Implementeret:** `SEC-P0-01A` erstatter Supabase-nøgler som auth på observations-/monitoring-ruter med to uafhængige server-only secrets, timing-sikker sammenligning og eksplicit afvisning af publishable/anon/secret/service-role credentials. `.env` er fjernet fra tracking og ignoreres; `.env.example` indeholder kun tomme nøglepladsholdere. Ældre monitoring-dokumentation er rettet til samme kontrakt og blokerer deployment, indtil den faktiske cron-caller er verificeret og migreret.
- **Reproducerbarhed:** `packageManager`/Node-engine og synkron npm-lock er fastlagt. En deterministisk mock-ledger-fejl med duplikerede sekvenser blev eksponeret ved baselinekørslen: samme sekvens/hash er nu idempotent, mens forskellig hash giver en eksplicit konflikt og bevarer den lagrede post. Lovable MCP-pluginet er afgrænset til understøttet non-Windows dev, og buildscriptet bruger 4 GB Node-heap.
- **Runtime/gates:** Node 22.14/npm 10.9.2. Frisk `npm ci` exit 0. `npm run typecheck` exit 0. Målrettede endpoint-/ledger-tests 27/27 og samlet Vitest 193/193 består. `npm run build` exit 0. Ændrede TypeScript-filer lint 0; global lint er fortsat rød med 5.428 errors og 25 warnings. npm audit: 17 advisories (2 low, 5 moderate, 10 high).
- **Browser/runtime-smoke:** `/` redirecter til `/login`; loginformularen renderes uden browserfejl. Browseren advarer om flere GoTrue-klienter med samme storage key. Begge sikrede endpoints returnerer 503 uden de nye secrets og rammer dermed ikke databasekoden.
- **Styring:** Ni P0-styringsfiler er oprettet med verificeret baseline og `AFVENTER` for ukendt live-tilstand. `SEC-P0-01A` er lokalt verificeret; secret-provisionering/deployment er ikke udført.
- **Git-checkpoint:** `61bf18b` indeholder runtime/npm/ledger, `106c825` indeholder endpoint-sikkerhed/env/cutover, og `142b9f4` indeholder P0-styringsbaselinen. `origin/main` var fortsat identisk med startcommit ved sidste fetch. Featurebranchen er pushet til `origin/codex/gofreyra-p0`; ingen PR, merge, deploy, live Supabase-skrivning eller produktionsændring er udført.
- **Planlagt fortsættelse:** Samme-chat-automationen `gofreyra-p0-90-min-cyklus` er oprettet aktiv med 90-minutters interval. Den må kun udføre én afgrænset, højest prioriteret ublokeret P0-opgave pr. cyklus, committe/pushe featurebranchen og skal pause sig selv, når hele P0-rejsen er dokumenteret bestået.
- **Næste:** `SEC-P0-01B` beskytter naturpersistens og projektscoper eksternt ingest. Derefter kræves Supabase dev/test-adgang til RLS-lockdown med to tenants; GoTrue-klienterne skal konsolideres.

## 2026-08-29 - manuel cyklus 002

- **Scope:** Afgrænset første halvdel af `SEC-P0-01B`: browserudløst natur-geodata og dens service-role-persistens. Observations-ingest blev auditeret, men ikke ændret i samme cyklus.
- **Implementeret:** `fetchAndIngestNatureGeo` kræver verificeret bearer-JWT, canonical Postgres UUID og eksplicit projektadgang. Direkte `admin`/`project_manager`/`editor` samt org owner/admin kan persistere; `field`/`viewer`/`external` er read-only. Ukendt/cross-tenant projekt afvises med generisk 403 før WFS og admin-load. Persistens bruger kun projektets serverlagrede danske centroid; caller-koordinater kan kun bruges read-only. Connector-loggen gemmer verificeret `claims.sub` som actor, og databasefejl kan ikke længere rapporteres som `persisted:true`.
- **Miljøkontrakt:** `.env.example` og setup-dokumentation angiver nu både browserens `VITE_*`-par og de nødvendige serveraliases `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY`; service-role forbliver server-only. Testen bekræfter fail-closed, hvis kun browseraliases findes.
- **Tests/gates:** 37/37 målrettede natur-/JWT-/rolle-/cross-tenant-/no-write-/persistenstests består. `npm run typecheck` exit 0; ændrede TypeScript-filer lint exit 0; samlet Vitest 29 filer og 222/222 tests; `npm run build` exit 0 med de kendte geotiff/chunk/Nitro-advarsler. `git diff --check` er ren.
- **Uafhængigt review:** Første review fandt preview-bypass, seed-UUID-regression, forkert persistensstatus og manglende auth-/cross-tenant-dækning; alle blev rettet og regressionstestet. Gen-review fandt ingen nye P0/P1 i patchen. En efterfølgende runtime-review fandt den manglende lokale serveralias-kontrakt, som også er rettet og testet.
- **NO-GO/blocker:** Den eksisterende `project_members` INSERT-policy tillader stadig self-insert med valgfri rolle og kan derfor omgå naturrollechecket. Effektiv live RLS, Storage og to-tenant-adfærd er ukendt, fordi den konfigurerede Supabase-instans ikke er tilgængelig. Denne cyklus er kun GO til featurebranch commit/push; ikke til deploy eller afslutning af `SEC-P0-01B`.
- **Git-checkpoint:** `a13a1ae` (`fix(security): scope nature data persistence`) er pushet til `origin/codex/gofreyra-p0`. Ingen PR, merge, deploy, live migration eller produktionsændring er udført.
- **Næste:** Scope observations-ingest til en verificeret integration-/projektidentitet og valider `site_id`/`source_id`; derefter luk self-insert/legacy policies og kør live negative tests med to organisationer.

## 2026-08-29 - planlagt cyklus 003

- **Scope:** Afgrænset resterende lokale del af `SEC-P0-01B`: observations-ingest må ikke længere bruge et globalt credential med caller-valgt projekt eller fremmede site-/datakilder.
- **Implementeret:** `OBSERVATIONS_INGEST_API_SECRET` bindes server-side én-til-én til et canonical `OBSERVATIONS_INGEST_PROJECT_ID`. Manglende/ugyldigt scope giver 503, og credential A mod body-projekt B giver 403 før admin-import/database. Projektet skal findes med organisation; alle unikke site-/source-ID'er normaliseres, deduplikeres og valideres i chunks mod samme projekt før én bulk-insert. Insert og efterfølgende aggregation bruger kun det serverkonfigurerede projekt.
- **Miljø-/driftskontrakt:** `.env.example`, arkitektur, runbook og ADR-015 dokumenterer ét runtime, ét credential og ét projekt. Scopeskift kræver samtidig credentialrotation; en senere flerprojektmodel kræver særskilt design.
- **Tests/gates:** TDD viste først 6 forventede fejl i den nye scope-suite. Derefter består 32/32 målrettede endpoint-/scope-tests, herunder missing/malformed scope, A→B uden DB, cross-project relationer, blandet batch uden delvis skrivning, deduplikering, canonical GUID og insertfejl. `npm run typecheck` exit 0; ændrede TypeScript-filer lint exit 0; samlet Vitest 30 filer og 234/234 tests; `npm run build` exit 0 med kendte geotiff/chunk/Nitro-advarsler. Global lint er fortsat rød med 5.407 errors og 25 warnings.
- **Uafhængigt review:** Arkitektur-, test- og sikkerhedsreview støtter den afgrænsede én-projektmodel. Reviewets fund om legacy Postgres-GUID'er, lowercase-normalisering, org-check, batch-atomaritet og chunkede opslag er implementeret. Ingen ny lokal P0-bypass blev fundet ved gen-review.
- **NO-GO/blocker:** Routevalidering og insert er ikke én database-transaktion, fordi schemaet mangler composite project/relation constraints; et lille TOCTOU-gab består. `project_members` self-insert, legacy policies og effektiv RLS/Storage er stadig uverificerede. Supabase dev/test og secret-provisionering er utilgængelig, så live evidens, migration og deploy er **AFVENTER**.
- **Git-checkpoint:** `29d0845` (`fix(security): scope observations ingest`) er pushet til `origin/codex/gofreyra-p0`. Ingen PR, merge, deploy, live migration eller produktionsændring blev udført.
- **Næste:** `SEC-P0-02` kræver Supabase dev/test-adgang til sikker schema-/RLS-lockdown og to-tenant-tests. Hvis blokeringen består, vælges næste højest prioriterede ublokerede lokale P0-opgave.

## 2026-08-30 - brugerbestilt cyklus 004

- **Udgangspunkt:** Branch `codex/gofreyra-p0`, HEAD `29d0845`, uden remote-divergens ved cyklusstart. `SEC-P0-02` var fortsat blokeret af manglende Supabase dev/test-adgang. Brugeren bestilte specifikt et droneflow, der udtrækker og bevarer geodata fra billeder før Før/Efter-monitorering.
- **Scope:** Én lokal, afgrænset del af `BA-P0-01`: tabsfrit, versionsfast metadataudtræk og fail-closed kvalitetssikring fra originalt dronefoto ind i den eksisterende uploadvalideringskø. Ingen migration, live Storage, deploy, ortofotogenerering eller ny parallel app.
- **Implementeret:** `exifr` er fastlåst til 7.1.3. Servicen beregner originalens SHA-256, bevarer JSON-sikker rå EXIF samt rå/parset XMP og normaliserer UTC/lokal tid, GPS, nøjagtighed, datum, højdeformer, retning/orientering, kamera, RTK og footprint-readiness. Koordinatpar og tidsfelter vælges inden for samme metadatakilde; ugyldige kalendere/tider og parser-/sidecarfejl bliver eksplicit QA. UploadWizard gemmer hele envelopen i `uploads.detected_metadata` og viser status. Projektmedieupload sender dronefotos til valideringsflowet og bruger ikke længere projektcentroid som standardposition.
- **Kvalitet/review:** Første uafhængige review fandt risiko for blandede lat/lon-kilder, tid/offset fra forskellige kilder og skjulte parserfejl. Alle tre blev rettet og dækket af regressionstests. En programmatisk minimal JPEG med reel APP1/EXIF validerer den faktiske parservej med kendt GPS, tid og kamera. Binære MakerNotes bevares i originalfilen og repræsenteres kun ved type/byteantal i JSON; de vendor-dekodes ikke.
- **Tests/gates:** 32/32 målrettede upload-/metadata-tests, `npm run typecheck` exit 0, målrettet lint på alle ændrede kodefiler exit 0, samlet Vitest 32 filer og 259/259 tests, og `npm run build` exit 0. Kendte geotiff/chunk/Cloudflare-advarsler består som ikke-fejl.
- **Browser:** Interaktiv lokal accept er **AFVENTER**. Den tilgængelige browser afviste `http://127.0.0.1:4173/login` af URL-sikkerhedspolitik; der blev ikke brugt en omgåelse, og det tælles ikke som observeret produktfejl eller bestået E2E.
- **NO-GO/blocker:** Metadata ligger kun i staging. Routing til `drone_assets`, privat projekt-/flight-bucket, resumable batchupload, duplikat-idempotens, feltreview, footprint/ortofoto og live RLS-/Storage-tests er ikke gennemført. Eksisterende åbne/self-insert policies gør fortsat deploy **NO-GO**.
- **Git-checkpoint:** `0f2afbd` (`feat(drone): validate geospatial image metadata`) er pushet til `origin/codex/gofreyra-p0`. Ingen PR, merge, deploy, live migration eller produktionsændring blev udført.
- **Næste:** Start den bestilte 4DM Project Monitor-audit mod den faktiske TanStack/React/Vite/Leaflet/Supabase-platform, dokumentér gap matrix og vælg kun den højeste ublokerede vertikale P0-del.

## 2026-08-30 til 2026-08-31 - brugerbestilt cyklus 005

- **Udgangspunkt:** Branch `codex/gofreyra-p0`, baseline `0f2afbd`, samme commit som remote ved start. Dronecheckpointet fra cyklus 004 var allerede pushet. Brugeren bestilte udførelse af 4DM Project Monitor-briefet i den eksisterende platform.
- **Audit:** Den faktiske stack, routes, services, migrations/policies, Storage, rapporter, tests og dokumentation blev kortlagt. Platformen har mange genbrugelige byggesten, men geometri-, kort-, connector-, medie-, audit- og rapportveje er fragmenterede. Kritiske fund er legacy åbne/self-insert RLS-policies, uafklaret frisk migrationsreplay, manglende canonical survey/dataset/change/review/field-visit/intervention/report-version-domæner og flere parallelle eksport-/connectorveje.
- **Valgt vertikal slice:** `GEO-P0-01`, fordi kunderejsen stoppede allerede ved projektområdet: en gemt grænse lignede redigerbar UI, men kunne ikke faktisk gemmes efter redigering, og eksport kunne blande legacy-/demoindhold ind. Der er ikke bygget en parallel platform eller tilføjet P1/P2-funktioner.
- **Implementeret:** Én fail-closed Polygon-validator håndterer ringe/huller, topologi, areal/centroid og størrelsesgrænser. Eksisterende grænser har eksplicit Gem/Annuller, lagfrysning under skrivning, synlig retry og nulstilling ved prop-skift. Ugemte edits blokerer tegning, upload, clear, projektskift og canonical download. Persistence bruger én canonical service, validerer den returnerede række før success og rydder alle afledte felter ved clear uden at genoplive matchende seed-geometri. Eksportpipeline (`geospatial-service` + `map-export-service`) dybdevaliderer seks GeoJSON-geometrityper, project truth og nested koordinater; 200 eller flere observationsfeatures afvises som mulig RPC-afkortning. Canonical observations-CSV escape'r felter og neutraliserer formelinjektion; Connect metrics-/zone-CSV er separate udtræk uden samme garanti.
- **Forsknings-/metodekontrakt:** Vandløbsmetoden afgrænser, hvad RGB-dronefotos kan dokumentere, og hvad der kræver ortofoto/fotogrammetri, feltdata, hydraulik eller faglig review. EXIF-kameraposition behandles ikke som footprint/pixelgeoreference. Gentagne Før/Efter-surveys kræver dokumenteret AOI, fase, GSD/overlap, CRS/datum, kontrolpunkter, udstyr/software, vejr/hydrologi og afvigelser.
- **Uafhængigt review:** Reviewfund om Connects legacy-eksport, store filers/Polygoners ressourceforbrug, styrede tegnetilstands-races, ugemte edits, seed-after-clear, stale eksportareal, overfladisk RPC-validering, mulig 200-feature-afkortning, CSV-formelinjektion og persistence/readback/stale-success er rettet og regressionstestet. Metrics-cachen invalideres ved boundary save/clear, og manglende/ugyldigt `calculated_at` afvises, men metrics kan ikke bindes til den aktuelle boundary uden en schema-/RPC-version; det er ikke påstået løst.
- **Tests/gates:** 8 målrettede filer og 68/68 geometri-/persistens-/eksporttests består. En ren solo-fuldkørsel består med 37 filer og 308/308 tests; en tidligere parallel kørsel ramte timeoutstøj i urørte natur-/ledger-tests og tæller ikke som produktfejl efter den grønne solo-genkørsel. Typecheck og produktionsbuild består. Berørte TypeScript-filer har 0 lintfejl og 2 kendte Fast Refresh-warnings. Global lint er fortsat rød med 5.407 errors og 25 warnings.
- **NO-GO/blocker:** Ingen migration eller live ændring er udført. Immutable boundary-revision, et atomisk/versionsbundet boundary+RPC-snapshot, metrics-friskhed mod boundary-version, cross-tab concurrency, rigtig DB-readback, browserrejse, Storage og negative to-tenant-RLS-tests er **AFVENTER**. Supabase-instansen er fortsat utilgængelig, og deploy er **NO-GO**.
- **Git-checkpoint:** Den sammenhængende kode-, test- og dokumentationsændring committes og pushes kun til `codex/gofreyra-p0`. Ingen PR, merge, deploy, live migration eller produktionsændring udføres.
- **Næste:** Hvis Supabase dev/test bliver tilgængelig, lukkes `SEC-P0-02` først. Ellers følges backloggen med auth/session-konsolidering før versionsfast boundary/survey/flight/dataset og privat Storage.

## 2026-08-31 - brugerbestilt cyklus 006

- **Udgangspunkt:** Branch `codex/gofreyra-p0`, baseline `777b05b`. Remote fetch består, og `HEAD...upstream` er `0/0` på samme commit. Worktree var ren ved cyklusstart og er nu dirty med cyklusændringerne. Brugeren bestilte eksplicit audit og hærdning af 4DM Supabase, RLS og tenant-isolation.
- **Live-/miljøaudit:** Konfigureret project ref er `ikrmcetjutqcjtwfhzfv`, men Supabase-connectoren svarer `permission denied`. Det eneste projekt, connectoren kan liste, er et andet, inaktivt projekt; det er ikke brugt eller ændret. Der er ingen service-role-/ingest-/monitoring-secrets i det lokale miljø. Supabase CLI 2.116.0 er fastlåst, men `npm run supabase:start` stopper med `docker: command not found` og finder heller ikke Podman.
- **Kildesikring:** En ny, endnu ikke anvendt migration samler caller-bundne read/write/manage-helpers i et låst `private`-schema, fjerner kendte legacy/open policies og anonym tabeladgang, lukker `project_members` self-enrolment og org-owner-opgradering, gør membership-/tenantnøgler, `uploads.uploaded_by` og indirekte child-parentnøgler immutable mod cross-tenant re-parenting og afgrænser uploads/import-jobs til personal/project/org-scope uden reassignment. Uploadens `storage_path` bindes til uploaderen, valideres som sikker path, gøres immutable og beskyttes mod dubletter og samtidige inserts med en transaktionslås. Feltrollen er hærdet til en eksplicit indsamlings-/evidenswhitelist, frem for generel editoradgang. `external` fejler lukket og kan ikke læse hele projektet, før der findes en autoritativ document-share-relation; canonical Storage-path og metadatareferencer validerer organisation→projekt-match, og etablerede metadata-paths er immutable. Generic same-project guards, action-evidence/device-parameter guards og en fail-closed polymorf quality-scope guard afviser krydsprojekt-proveniens; `data_quality_issues.measurement_id` får tillige `NOT VALID` FK med `ON DELETE SET NULL`. Private Storage-buckets har ingen `UPDATE`-policy; almindelig monitoring-delete kræver manage, mens orphan-cleanup er begrænset til ejerens objekt i contributor-scope, højst 15 minutter gammelt og uden metadata-række. Kolonnegrants på både `INSERT` og `UPDATE` reserverer afledt uploadmetadata, validerings-/importresultater og status til backend, mens uploadoprettelse auditeres af en DB-trigger. `handle_new_user` har ikke længere e-mail-baseret shared demo-owner-eskalering og opretter profil plus isoleret personal organisation ved signup. Projekt-RPC'er beskyttes; tre buckets er private; og tidligere uploadere mister Storage-adgang ved medlemsfjernelse. Seed og legacy bootstrap opretter ikke længere `dev_all` eller redefinerer projekt-RPC'er; `006_project_media.sql` bruger nu den korrekte UUID-FK ved frisk replay. GitHub-workflowet er ændret fra automatisk migrationspush på `main` til manuel produktionsdispatch med `DEPLOY`-bekræftelse, environment gate, secret-båret project ref og dry-run.
- **App-lag:** React Query-cache og valgte tenant-ID'er ryddes ved account switch/logout; resultater fra en tidligere sessions asynkrone profil-/membershipload kasseres; og direkte org-/projektvalg kræver match i indlæste memberships. Connect accepterer kun URL-/persisted projektvalg, der findes i den aktuelle organisations projekter. Projektmedier og monitoring-downloads materialiseres fra DB-autoriserede paths med fast signed-URL-TTL på 300 sekunder og ignorerer persisterede public URLs; delete accepterer ikke caller-path, stopper ved Storage- eller metadatafejl og auditerer først efter bekræftet sletning. Evidence-upload sanitiserer filnavn og gemmer kun privat path. Monitoring-upload sender browserens preview/EXIF som ubetroet `user_metadata`, ikke som afledt metadata; ved fejlet metadata-insert forsøges Storage-rollback, og en rollbackfejl er synlig. De to GoTrue-klienter er ikke konsolideret.
- **Testartefakter og observerede app-gates:** En transaktionel pgTAP-suite med `plan(58)` dækker to organisationer, admin/viewer/field/external/outsider, direkte CRUD, self-enrolment/owner-promotion, founder-e-mail uden demo-owner-eskalering, isolated personal-org signup, immutable membership-/tenantnøgler og child-parent-/uploader-path-identitet, upload/import-job scope, backend-ejet afledt metadata, DB-uploadaudit, feltrolle-whitelist, to negative external-read assertions, projekt-RPC'er, same-project relationer, polymorf quality scope, measurement-FK, forged canonical organisation/projekt-Storage-path, privat Storage rename-afvisning, negativ action-evidence re-parenting, field orphan-cleanup, anonym adgang og Storage-adgang efter medlemsfjernelse; den er ikke kørt. Målrettet Vitest: 5 filer/36 tests, exit 0. Fuld direkte Vitest: 42 filer/344 tests, exit 0. Typecheck og produktionsbuild: exit 0; build har kun kendte bundler-advarsler. Målrettet ESLint fra repository-roden: 0 errors/3 Fast Refresh-warnings. Global lint: 394 filer, exit 1 med 5.164 errors/23 warnings. `git diff --check`: exit 0.
- **DB-gates og NO-GO:** `supabase:start` stopper uden Docker/Podman; reset exit 1 med `LegacyLocalDbRunningError`; pgTAP og DB-lint exit 1 med `ECONNREFUSED 127.0.0.1:54322`. Frisk replay, 58 pgTAP-cases, DB-lint, Auth/PostgREST, Storage list/read/write/delete/`UPDATE`-afvisning/signed URLs og live to-tenant-adfærd er ikke verificeret. Storage `INSERT` mangler et serverudstedt upload-intent bundet til præcis én pending metadata-række, buckets mangler størrelse-/MIME-constraints, og orphan reconciliation/retention er ikke implementeret. Et allerede udstedt signed bearer-link kan bruges indtil udløb, højst 300 sekunder efter access revoke. Der er ikke anvendt migration, skrevet testdata, deployet, roteret secrets eller rørt et live Supabase-projekt. `SEC-P0-02`, P0 og deploy er fortsat blokeret/**NO-GO**.
- **Git-checkpoint:** Cyklusændringerne forbliver lokale og ucommittede; de pushes ikke, fordi brugerens DB/Auth/PostgREST/Storage-accept ikke er opfyldt. Ingen PR, merge, deploy, live migration eller produktionsændring er udført.
- **Næste:** Skaff en disponibel Docker/Podman-baseret lokal Supabase-stack og gør frisk reset, pgTAP og DB-lint grønne. Få derefter adgang til det korrekte klassificerede dev/test-projekt og udfør negative CRUD-/RPC-/URL-/Storage-tests med to organisationer før migration/deploy overhovedet overvejes.

## 2026-08-31 - heartbeat cyklus 007

- **Scope:** Luk den konkrete bucket-constraint-del af `SEC-P0-02` i migrationskilden uden at opfinde en evidence-kontrakt eller påstå runtimeadfærd.
- **Kildesikring:** Hardening-migrationen konfigurerer `monitoring-uploads` til `209715200` bytes og den eksisterende monitoring-MIME-allowlist samt `project-media` til `52428800` bytes og `image/*`/`application/pdf`. `ON CONFLICT` genanvender privacy, `file_size_limit` og `allowed_mime_types` på eksisterende buckets. `evidence-files` forbliver privat, men eksisterende size/MIME-constraints bevares, fordi der ikke findes en godkendt evidence-kontrakt. En statisk migrationsassertion dækker begge konfigurerede buckets og denne fail-closed evidence-adfærd.
- **Observerede app-gates:** Målrettet Vitest 5 filer/37 tests PASS; fuld Vitest 42 filer/345 tests PASS; typecheck PASS; ESLint på den ændrede statiske test 0 fund; build PASS med kendte warnings. Global lint er uændret FAIL på 394 filer/5.164 errors/23 warnings. pgTAP-filen har `plan(62)` og 62 assertions, men runtime er **AFVENTER**.
- **Friske DB-forsøg:** Lokal reset stopper med `LegacyLocalDbRunningError`; pgTAP og DB-lint stopper med `ECONNREFUSED 127.0.0.1:54322`; Docker/Podman mangler fortsat. Ingen database-/Storage-adfærd er derfor verificeret.
- **NO-GO/blockere:** Serverudstedt upload-intent med eksakt pending-row-binding, godkendt evidence size/MIME-kontrakt, orphan reconciliation/retention, signed-URL-revocation-vinduet og alle lokale/live runtimegates består. P0 og deploy er fortsat **NO-GO**.
- **Git-checkpoint:** Worktree forbliver dirty, lokal og ucommittet. Ingen commit, push, PR, merge, deploy, migration eller live ændring er udført.

## 2026-08-31 - heartbeat cyklus 008

- **Udgangspunkt:** Branch `codex/gofreyra-p0`, baseline/upstream `777b05b`; frisk remote-kontrol viste `HEAD...upstream` `0/0`. De kendte cyklus 006/007-ændringer blev bevaret. `SEC-P0-02`'s database-/live-trin var fortsat blokerede, så den højest prioriterede ublokerede lokale del af `AUTH-P0-01` blev valgt.
- **Scope/implementering:** Browseren har nu én canonical Supabase-/GoTrue-klient i `src/lib/supabase/client.ts`. Den genererede integrationsmodulvej er en fail-fast compatibility Proxy til samme instans. `VITE_SUPABASE_PUBLISHABLE_KEY` har prioritet; tom/manglende publishable falder tilbage til legacy `VITE_SUPABASE_ANON_KEY`, og serverens `SUPABASE_*`-værdier kan ikke konfigurere browserklienten. Auth persistence, auto-refresh og URL-session-detektion er aktive i browserkontekst og deaktiverede ved SSR-evaluering.
- **TDD/review:** Den første env-resolution-kørsel fandt den forventede anon-fallback-fejl (1 af 3 fejlede); implementationen blev rettet, og regressionstesten blev udvidet til browser-/SSR-options. Uafhængigt re-review fandt ingen patch-introduceret P0/P1.
- **Observerede gates:** Browserklient 5/5; målrettet Vitest 4 filer/18 tests PASS; fuld Vitest 43 filer/350 tests PASS; typecheck PASS; ESLint på tre berørte filer 0 fund; build exit 0 med kendte warnings. Global ESLint er fortsat FAIL: 395 filer i alt, 174 med fund, 5.155 errors og 23 warnings. Frisk `/login` viste korrekt titel og synlig loginoverskrift, browserkonsollen havde 0 warnings/errors, og duplicate-GoTrue-advarslen var fraværende.
- **NO-GO/blockere:** Ingen credentials eller login/reset/logout/refresh/account-switch blev udført. Eksisterende SSR route-loaders bruger fortsat en global publishable/anon-klient uden request-scoped bruger-JWT; autentificeret initial navigation og RLS-samspil er derfor **AFVENTER** en særskilt serverklientkontrakt og dev/test-adgang. DB-/Storage-blokeringerne er uændrede; pgTAP har fortsat `plan(62)`/62 assertions, som ikke er kørt.
- **Git-checkpoint:** Worktree forbliver dirty, lokal og ucommittet. Ingen commit, push, PR, merge, deploy, migration eller live ændring er udført.
- **Næste:** Verificér login/reset/logout/refresh/account-switch samt request-scoped SSR-user-JWT, autentificeret initial navigation og RLS i det korrekte dev/test-projekt. Frisk migration replay, pgTAP, DB-lint og negative to-tenant-/Storage-tests forbliver foran ny domæneudvikling.

## 2026-08-31 - brugerbestilt cyklus 009

- **Mandat/miljø:** Brugeren autoriserede genbrug af det ellers ubrugte Supabase-projekt `xdvqdzdpyceojbdknofi` som isoleret GoFreyra P0-staging. Projektet blev reaktiveret til `ACTIVE_HEALTHY`. Lovable-/produktionsref `ikrmcetjutqcjtwfhzfv` blev kun brugt som read-only schemahistorisk reference og er ikke ændret.
- **Preflight:** Staging havde 0 Auth-brugere, 0 Storage-objekter/buckets og 45 kendte faste demo-rækker (`GoFreyra Demo Org` og tre demo-projekter). Fuld migrationsmappe blev afvist som unsafe pga. legacy-duplikater og `007`-FK-seed. Den valgte July-sekvens, `project_media`-reconciliation og hardening blev kørt i én transaktion med katalogassertions og `ROLLBACK`; efterfølgende fingerprint viste uændret baseline.
- **Anvendt staging-schema:** Den atomiske migration `20260831172610 bootstrap_gofreyra_p0_staging_20260831` og forward migration `20260831172839 validate_project_media_metadata_20260831` er registreret. Resultat: 61 app-tabeller med RLS, 0 `dev_*`-policies, tre private buckets, otte drone-/mediemetadatakolonner og otte validerede kategori/source/status/coordinate/range/nonnegative constraints. Demoindholdet blev bevaret; ingen testbruger eller Storage-objekt blev efterladt.
- **Live sikkerhedsevidens:** En transaktionel synthetic tenant A/B-test som `authenticated` beviste own-project/media/Storage-read samt cross-tenant project/media/Storage-read, write og metrics-RPC-afvisning; alle fixtures blev rullet tilbage. Anon PostgREST returnerer 401 for `projects` og `project_media`. Supabase security advisor har fortsat ét ERROR: `public.spatial_ref_sys` uden RLS; anon GET returnerer 200. Dette er synliggjort som ejerbeslutning og er ikke auto-rettet. PostGIS-in-public og SECURITY DEFINER-advisorwarnings kræver separat triage.
- **Staging-lane:** `.env.staging.local` er ignoreret; preflight kræver eksakt staging-URL/ref/key-par og viser ingen nøgler. `build:staging` afviser legacy/fremmede 20-tegns Supabase-refs og læk af konfigurerede server-only værdier. Cloudflare har navngivet `staging`-miljø; `preview:staging` kører det byggede Worker-output med lokale runtime bindings og kan ikke ramme den generiske Worker ved et almindeligt preview.
- **App/browsergates:** Typecheck PASS; fuld Vitest 43 filer/350 tests PASS; staging-build og bundle-ref/secret-scan PASS; målrettet ESLint 0 errors/3 kendte Fast Refresh-warnings; `git diff --check` PASS. Lokal Worker-preview på `http://127.0.0.1:4174/login` viser login og signup-UI; `/app` redirecter til `/login` uden session; 0 console errors. Der blev ikke oprettet konto eller sendt e-mail.
- **NO-GO:** Produktion, PR, merge og Lovable-deploy er ikke udført. Frisk lokal reset/62 pgTAP/DB-lint mangler fortsat Docker/Podman. Rigtig bruger-JWT login/logout/refresh, request-scoped SSR, Storage API upload/list/read/delete/signed-URL/revoke, serverudstedt upload-intent, evidence-kontrakt og orphan reconciliation er `AFVENTER`. `spatial_ref_sys` kræver ejerbeslutning før yderligere remediation.
- **Git-checkpoint:** Cyklus 006-009 samles i ét reviewbart featurebranch-checkpoint og pushes kun til `origin/codex/gofreyra-p0`; ingen merge eller produktionsændring.
