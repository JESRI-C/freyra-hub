# GoFreyra execution state

Opdateret: 2026-09-02, heartbeat P0-cyklus 014.

## Seneste verificerede checkpoint

- Repository/remote: `JESRI-C/freyra-hub`; branch `codex/gofreyra-p0`.
- Baseline HEAD for cyklus 014 er `7a7ca84`; branch og `origin/codex/gofreyra-p0` var synkroniserede (`0/0`), og worktree var ren før ændringen.
- Betroet orphan-reconciliation er implementeret i kilden for annullerede eller udløbne, ikke-modtagne upload-intents. En `service_role`-eksklusiv RPC tager højst 100 korte `FOR UPDATE SKIP LOCKED`-claims med opaque lease-token; den dedikeret secret-beskyttede serverroute sletter kun den eksakte, validerede `monitoring-uploads`-path via Supabase Storage API og kvitterer success eller en saniteret fejl i en privat cleanup-ledger. Udløbne drafts arkiveres før sletning, stale workers kan ikke afslutte et nyere claim, og SQL ændrer aldrig `storage.objects`.
- P0-retentionen er fail-closed: det fysiske orphan-objekt slettes permanent via Storage API, mens den arkiverede uploadrække og private cleanup-ledger med attempt-/completionstatus bevares. Automatisk scheduling, live Storage-adfærd og eventuel senere ledger-purge er **AFVENTER** særskilt driftsmandat og runtime-evidens.
- Cyklus 014-gates efter ren installation: `npm ci --no-audit --no-fund` tilføjede 850 pakker på Node 22.14.0/npm 10.9.2; typecheck PASS; målrettet ESLint på seks berørte TypeScript-filer 0 fund; fem målrettede filer/36 tests PASS; fuld serial Vitest 54 filer/412 tests PASS; produktionsbuild PASS med kendte bundler-advarsler; `git diff --check` PASS. pgTAP-kilden er udvidet til `plan(105)`, men frisk replay, de 105 cases og DB-lint kan fortsat ikke starte, fordi lokal Postgres afviser på `127.0.0.1:54322` og Docker/Podman mangler. Ingen migration, schedule, deploy, live data, secret, produktion eller custom domain er ændret.
- Baseline HEAD for cyklus 013 er `865bf73`; branch og `origin/codex/gofreyra-p0` var synkroniserede, og worktree var ren før ændringen.
- Den projektbundne drone-FØR-modtagelse er implementeret i kilden som en batch på højst 200 filer med maksimalt to samtidige metadata-/hash- og uploadjobs. Hver fil får stabilt request-ID, rækkefølge og serverudstedt, kortlivet upload-intent; TUS bruger 6 MiB-chunks, genoptager kun samme eksakte intent/path og bruger aldrig overwrite/upsert. Browserens EXIF/XMP/GPS/UTC er fortsat ubetroet preview, manglende geodata giver aldrig centroid-fallback, og en fil er først `received`, når server-finalize har verificeret det eksakte Storage-objekt.
- Upload-intent-migrationen tilbagekalder direkte `authenticated`-insert i `uploads`, binder intent til actor/tenant/projekt/zone/fil/hashmetadata, låser scope og idempotens, begrænser aktive intents/rate, afviser rename/upsert og skjuler draft ved udløb eller tabt medlemskab. Zoner med uploadproveniens kan ikke slettes løsrevet. Annulering arkiverer metadata; orphan-reconciliation findes nu i kilden, mens scheduling og live runtime er **AFVENTER**.
- Cyklus 013-gates: typecheck PASS; målrettet ESLint på 14 berørte TypeScript-filer 0 fund; fuld serial Vitest 51 filer/397 tests PASS; produktionsbuild PASS med kendte bundler-advarsler; `git diff --check` PASS. Den versionsstyrede pgTAP-kilde har nu `plan(83)`, men frisk replay, pgTAP, DB-lint og rigtig Auth/Storage/TUS-browsertest kunne ikke køres uden lokal Docker/Podman og er **AFVENTER**. Ingen migration, deploy, live testdata, secrets, produktion eller custom domain er ændret.
- Baseline HEAD for cyklus 012 er `58b3434`; branch og `origin/codex/gofreyra-p0` var synkroniserede, og worktree var ren før ændringen.
- Brugerens credential-bårne staging-login nåede nu projektoversigten. Oprettelse som verificeret `owner` fejlede med Postgres `42501`, selv om `private.can_write_organization` var sand. Live API-/Postgres-log og en rollback-beskyttet SQL-diagnose isolerede årsagen: plain `INSERT` bestod, mens samme insert med `RETURNING` blev afvist, fordi `projects_read` genlæser den endnu ikke synlige række i samme command snapshot. Ingen policy blev lempet, og ingen diagnostisk række blev bevaret.
- Projektoprettelse bruger nu et klientgenereret UUID og plain `INSERT` uden representation/`RETURNING`; næste request genlæser de RLS-filtrerede organisationer/projekter og vælger kun det nye projekt, hvis det findes dér. Owner/admin/editor kan oprette; øvrige roller får ikke knappen. Rå databasefejl skjules, og en post-commit refreshfejl beskrives som oprettet projekt i stedet for at opfordre til en dublet.
- Cyklus 012-gates før deploy: 16/16 målrettede projekt-/selectiontests PASS; typecheck PASS; fuld serial Vitest 48 filer/378 tests PASS; målrettet ESLint 0 errors med tre kendte Fast Refresh-warnings; Prettier og `git diff --check` PASS; `build:staging`, staging-ref-/secret-preflight og Wrangler 4.90.0 dry-run mod det navngivne `staging`-miljø PASS. Uafhængigt re-review fandt ingen resterende P0/P1 i patchen.
- Commit `fb06415` er pushet til `origin/codex/gofreyra-p0` og deployet kun til Cloudflare Worker `gofreyra-p0-staging`, version `bab7b8fc-122a-46f3-ac97-1f6ec7cd1d77`. Hosted `/login` viser den komplette formular med korrekt titel; anonym `/app` redirecter til `/login`, og browserkonsollen har 0 warnings/errors. Produktion, Simply, custom domain, Supabase-schema og secrets er ikke ændret.
- En secretsfri og ikke-deployende P0 app-gate findes nu i `.github/workflows/ci.yml`. Den kører kun ved pull request, accepteret push til `main` eller manuel dispatch; checkout-credentials persisteres ikke, jobtokenet har kun `contents: read`, og workflowet bruger ingen repository-secrets, Supabase, Wrangler eller deploykommandoer.
- CI-lanen fastlåser Node 22.14.0/npm-lockvejen og kører `npm ci --no-audit --no-fund`, typecheck, checksum-/faktorverifikation, serial Vitest og normal build. En kildekontrakttest afviser deploy-/secret-/featurebranch-trigger-regressioner.
- Cyklus 011-gates: CI-kontrakt 3/3 PASS; typecheck PASS; `verify:faktorer` PASS; fuld serial Vitest 46 filer/362 tests PASS; ESLint og Prettier på nye filer PASS; YAML parse PASS; normal `npm run build` PASS med kendte bundler-advarsler. Første hosted Linux/GitHub-kørsel er **AFVENTER** PR eller særskilt manuel dispatch; ingen workflowrun, deploy eller anden ekstern ændring blev udløst.
- Supabase-authbootstrap er rettet, så `onAuthStateChange`-callbacken forbliver synkron og databasekald først køres deferred og generation-guarded. Seneste identitet vinder, stale jobs kan ikke ændre tenantstate/loading, og same-user token-events genindlæser ikke profil, medlemskab og projekter.
- Password-login navigerer ikke længere før AuthProvider har hydreret den verificerede bruger. Authbootstrap-fejl er synlige og kan retries, aktiv manuel refresh afmonterer ikke appen, og `next` accepterer kun en sikker same-origin relativ sti.
- Cyklus 010-gates: 9/9 nye auth-/loginregressionstests PASS; typecheck PASS; fuld serial Vitest 45 filer/359 tests PASS; målrettet ESLint 0 errors med tre kendte Fast Refresh-warnings; `build:staging` og Wrangler dry-run PASS.
- Commit `94b37f1` er pushet til `origin/codex/gofreyra-p0` og deployet kun til Cloudflare Worker `gofreyra-p0-staging`, version `fbc38ac0-935b-43ff-8917-5a071b02e131`. Hosted `/login` har korrekt titel/indhold og 0 konsollogs; anonym `/app` redirecter til `/login`. Produktion, Simply, custom domain, Supabase-schema og secrets er ikke ændret.
- Faktisk stack og centrale auth-, API-, migrations-, kort-, upload-, Før/Efter-, målings-, kilde- og rapportveje er statisk auditeret.
- Runtime er fastlåst til Node `>=22 <23` og npm 10.9.2. Cyklus 014 genverificerede den aktuelle lockfil med et frisk `npm ci --no-audit --no-fund` og genkørte typecheck, hele testpakken og produktionsbuild på den rene installation.
- `npm run typecheck`: exit 0 efter cyklus 008-ændringerne.
- ESLint på de tre ændrede klient-/testfiler: exit 0 uden fund. Seneste globale `npm run lint`: 395 filer, exit 1 med 5.155 errors og 23 warnings, hovedsageligt eksisterende repository-wide Prettier-gæld.
- Cyklus 008's målrettede browserklient-/auth-cache-/Connect-selection-/serverauth-suite består med 4 filer og 18/18 tests. Hele Vitest-suiten består med 43 filer og 350/350 tests.
- `npm run build`: exit 0 efter cyklus 008-ændringerne med kun de kendte bundler-advarsler. Buildscriptet giver Node 4 GB heap; Windows-dev springer den fejlende Lovable MCP-routegenerator over, mens de versionsstyrede MCP-ruter bevares.
- Natur-serverfunktionen bruger den eksisterende bearer-attacher og JWT-middleware, verificerer projektmedlem/org-admin før WFS/admin, reserverer persistens til editor+, bruger kun serverlagret centroid og registrerer den verificerede actor. Læserroller og manglende service-role er read-only.
- Observations-ingest er lokalt bundet til ét serverkonfigureret projekt pr. credential. Manglende/ugyldigt scope og credential A mod projekt B stopper før admin-klient/database; projektet skal have organisation, og alle site-/source-ID'er valideres samlet mod samme projekt før én atomisk bulk-insert.
- Dronefotos går nu gennem monitoring-uploadets valideringskø. Browserens SHA-256, versionerede parserpreview, rå EXIF/XMP, normaliserede tid/position/højde/retning/kamera/RTK og QA gemmes kun som ubetroet `uploads.user_metadata.client_preview`; en betroet serverekstraktor skal senere udfylde `detected_metadata`. Manglende eller modstridende geodata, ukendt UTC, parsefejl og manglende koordinater blokerer automatisk kortaktivering; der anvendes ingen projektcentroid som erstatning.
- 4DM-auditten er dokumenteret mod den faktiske TanStack/React/Vite/Leaflet/Supabase-platform. Første vertikale slice gør projektgrænsen fail-closed, reelt redigerbar og ventende på bekræftet persistence før success; ugemte edits blokerer konfliktende boundary-operationer, clear nulstiller afledte værdier uden at genoplive seed-geometri, og både projektkort og Connect bruger canonical, valideret GeoJSON uden skjult seed-/preview-fallback.
- Polygon-ringe og huller valideres for lukning, WGS84-range, dubletter, selvskæring, nulareal og indbyrdes topologi. Import begrænses til 2 MiB og 500 reelle vertices før dyre operationer; MultiPolygon og FeatureCollection afvises eksplicit. Areal og centroid beregnes centralt og eksportarealet afledes fra den validerede geometri.
- RPC-observationer dybdevalideres for Point, MultiPoint, LineString, MultiLineString, Polygon og MultiPolygon. Ukendt/ugyldig geometri, usand project-identitet og 200 eller flere observationsfeatures afvises, så et muligt afkortet udtræk ikke fremstår komplet. Canonical observations-CSV afledes kun af dette validerede GeoJSON; Connect metrics-/zone-CSV er separate udtræk.
- `SEC-P0-02`-migrationen fjerner kendte legacy/open policies, flytter caller-bundne authz-helpers til et låst `private`-schema, lukker `project_members` self-enrolment og owner-opgradering og håndhæver de dokumenterede tenant-, upload-, Storage- og RPC-grænser. Den er nu anvendt atomisk og registreret på den brugerautoriserede staging-instans `xdvqdzdpyceojbdknofi`; den er ikke anvendt på Lovable-/produktionsinstansen `ikrmcetjutqcjtwfhzfv`.
- Browserens tenantstate hærdnes lokalt: React Query-cachen tømmes ved konto-/sessionovergang og logout, stale async brugerdata ignoreres, org-/projektvalg afvises uden medlemskabs-match, og Connect accepterer kun et projekt-ID fra den aktuelle organisations indlæste projektliste. Browseren har nu én canonical Supabase-/GoTrue-klient i `src/lib/supabase/client.ts`; den genererede integrationsmodulvej er en fail-fast compatibility Proxy til præcis samme instans og opretter ikke parallel authstate. `VITE_SUPABASE_PUBLISHABLE_KEY` vinder, mens tom/manglende publishable falder tilbage til legacy `VITE_SUPABASE_ANON_KEY`; serverens `SUPABASE_*`-variabler konfigurerer aldrig browserklienten. Auth persistence, auto-refresh og URL-session-detektion er aktive i browserkontekst og deaktiverede ved SSR-evaluering.
- Projektmedier og monitoring-downloads materialiseres fra DB-autoriserede paths med signed URLs fastlåst til 300 sekunder; legacy public URLs ignoreres, og delete stoler ikke på caller-path. Monitoring-delete stopper, hvis objektet eller præcis én metadata-række ikke kan slettes, og audit sker først bagefter. Evidence-upload sanitiserer filnavne og gemmer kun privat object path. Mislykket metadata-insert udløser Storage-rollback, og en mislykket rollback bliver nu synlig for operatøren. Den eksisterende migrationsworkflow er ændret fra automatisk push på `main` til manuel produktionsdispatch med `DEPLOY`-bekræftelse, environment gate, secret-båret project ref og dry-run.
- Bucket-konfigurationen i hardening-migrationen sætter `monitoring-uploads` til `209715200` bytes med den eksisterende MIME-allowlist og `project-media` til `52428800` bytes med `image/*`/`application/pdf`; `ON CONFLICT` genanvender privacy, størrelsesgrænser og MIME-allowlists på eksisterende buckets. `evidence-files` gøres privat uden at overskrive eksisterende size/MIME-constraints, fordi en autoritativ evidence-kontrakt ikke er godkendt.
- Supabase CLI er fastlåst til 2.116.0 med lokale reset/test/lint-scripts. Den statiske hardening-, upload-intent-, orphan-reconciliation- og app/service-regression er grøn i den fulde 54-filers/412-tests suite. Den transaktionelle pgTAP-fil har `plan(105)` med intent-, idempotens-, immutability-, legacy-, udløbs-, medlemskabs-, zoneproveniens-, service-role-, lease-, retry-, stale-token-, orphan-retention- og received-delete-assertions, men er **ikke kørt**.
- Frisk browser-smoke: `/login` renderes med korrekt titel og synlig loginoverskrift, konsollen har 0 warnings/errors, og duplicate-GoTrue-advarslen er fraværende. Der blev ikke anvendt credentials eller udført login/reset/logout/refresh/account-switch.
- Begge sikrede endpoints returnerer 503 før databaseadgang, når deres nye dedikerede secrets mangler.
- Staging-checkpoint: projektet er `ACTIVE_HEALTHY`; migrationerne `20260831172610 bootstrap_gofreyra_p0_staging_20260831` og `20260831172839 validate_project_media_metadata_20260831` er registreret. 61 app-tabeller har RLS, `dev_*`-policies er 0, tre buckets er private, otte drone-/mediemetadatakolonner og otte validerede constraints findes. Ingen Auth-brugere eller Storage-objekter blev efterladt. De 45 bevarede rækker er den kendte faste demoorganisation og dens demoindhold.
- Cyklus 009-gates: typecheck PASS; fuld Vitest 43 filer/350 tests PASS; staging-build PASS og indeholder kun staging-ref; transaktionel schema-dry-run med katalogassertions PASS; live SQL-tenanttest beviste A-eget read samt B-read/write/RPC/Storage-afvisning og rullede alle fixtures tilbage. Anon PostgREST giver 401 for `projects` og `project_media`; `spatial_ref_sys` giver fortsat 200 og kræver en særskilt ejerbeslutning.
- Lokal Worker-preview kører via det byggede Cloudflare-output på `http://127.0.0.1:4174/login`. Login/signup renderes, `/app` redirecter til `/login` uden session, og konsollen er uden errors. Ingen rigtig konto eller e-mail blev oprettet.
- Verificerede Git-checkpoints før denne cyklus: `61bf18b`, `106c825`, `142b9f4`, `a13a1ae`, `29d0845`, `0f2afbd` og `777b05b`. Ingen PR, merge eller produktionsændring er udført.
- Samme-chat-automationen `gofreyra-p0-90-min-cyklus` er aktiv og fortsætter hver 90. minut med én afgrænset P0-opgave, de dokumenterede gates og automatisk stop/pause efter bestået P0.

## Aktiv højeste opgave

`SEC-P0-02` er fortsat den højeste aktive sikkerhedsgate. Serverudstedt upload-intent, eksakt pending-row-binding, projektbundet resumérbar FØR-batch og betroet orphan-reconciliation er implementeret og app-/kildetestet, men de to forward migrationer er ikke anvendt på staging. Workerpathen bindes til claimets upload-ID, routefejl logges uden rå providerdata, og ikke-modtagne orphan-intents/ledger-audit kan ikke slettes uden en ny retentionmigration; modtagne uploads bevarer normal manage-delete. Rigtig Storage TUS/API/cleanup, signed-URL/revoke, lokal frisk replay/105 pgTAP-cases, cleanup-scheduling og `spatial_ref_sys`-beslutningen mangler. Den samlede Før/Efter-rejse og 4DM Project Monitor er ikke bestået.

## Aktive blokeringer

1. Den scoped observationsvalidering og efterfølgende insert er ikke én database-transaktion; schemaet mangler composite relation constraints. Endelig TOCTOU-lukning kræver sikker migration/RPC og live preflight i `SEC-P0-02`.
2. Legacy åbne policies og `project_members` self-insert er lukket på staging og negativt SQL-testet. Frisk lokal reset/105 pgTAP-cases og rigtig Auth/PostgREST/Storage API-accept mangler fortsat, så produktion er **NO-GO**.
3. `xdvqdzdpyceojbdknofi` er brugerautoriseret staging og må anvendes til test. Lovable-/produktionsinstansen `ikrmcetjutqcjtwfhzfv` er fortsat read-only/uden connectoradgang og er ikke ændret.
4. Den canonical browserklient, authbootstrap-race-/deadlockrettelsen og hosted anonym staging-smoke er verificeret, men credential-båret login/reset/logout/refresh/account-switch er fortsat **AFVENTER** brugerens staging-smoke. Signup-/bekræftelseslinks kræver desuden korrekt Supabase Auth Site URL/redirect-allowlist; Worker-deployet ændrer ikke dette. Eksisterende SSR route-loaders bruger fortsat en global publishable/anon-klient uden request-scoped bruger-JWT; autentificeret initial navigation og RLS-samspil kræver derfor også en eksplicit serverklientkontrakt.
5. Global lint er rød, og npm audit rapporterer 17 advisories (2 low, 5 moderate, 10 high). Buildet består, men store chunks og bundler-advarsler mangler triage.
6. Endeligt Haderslev/Skallebæk-projektnavn og reelt P0-datasæt er **AFVENTER** projektmaster.
7. Projektbundet FØR-batch, TUS-resume og idempotent intent-håndtering er implementeret lokalt, men uploadkøen er endnu ikke routet til den kanoniske `drone_assets`-model eller survey rounds/photo pairs. Live RLS-/Storage-/TUS-tests er **AFVENTER**.
8. Et dronekamerapunkt er ikke et footprint eller en ortofoto-georeference. Objektiv/sensor/GSD/footprint og eventuel fotogrammetri kræver verificerede input og må ikke udledes ved gæt.
9. Lokal login-, signup-UI- og auth-redirect-smoke er bestået på Worker-previewen. Droneuploadens fulde browserrejse med rigtig konto/fil er fortsat **AFVENTER**.
10. Projektgrænsen har endnu ikke immutable revisioner eller versionskolonne til cross-tab/to-bruger optimistic concurrency. Den lokale hook afviser samtidige writes i samme UI-instans, men en komplet løsning kræver schema-/RPC-ændring og live verifikation.
11. Canonical geometri er fortsat et enkelt GeoJSON `Polygon`; `MultiPolygon` og `FeatureCollection` er eksplicit unsupported i denne slice.
12. Boundary-rækken og RPC-feature-samlingen læses ikke atomisk eller mod samme version. Metrics-cachen invalideres på boundary save/clear, og ugyldigt `calculated_at` afvises, men det aktuelle schema har ingen boundary-/source-version, som kan bevise metrics-friskhed. Begge garantier er **AFVENTER** schema-/RPC-versionering og live verifikation.
13. Lokal Supabase kan ikke startes i dette miljø: `supabase:start` stopper med `docker: command not found` og finder heller ikke Podman; reset stopper med `LegacyLocalDbRunningError`; pgTAP og DB-lint stopper med `ECONNREFUSED 127.0.0.1:54322`. Derfor er frisk migration replay, de 105 planlagte pgTAP-cases, DB-lint og Storage-adfærd **AFVENTER** et disponibelt container-runtime.
14. Storage `INSERT` er i migrationskilden bundet til et serverudstedt upload-intent og præcis én pending metadata-række, og orphan-reconciliation har nu en privat lease-ledger samt en serverroute, der kun sletter eksakte paths via Storage API. Staging kører ingen af disse forward migrationer eller den nye route. Rigtig Storage API/TUS upload/delete/cleanup/signed-URL/revoke, automatisk cleanup-scheduling og en eventuel ledger-purgepolitik mangler; `evidence-files` mangler fortsat en godkendt size/MIME-kontrakt.
15. Supabase-advisors rapporterer `public.spatial_ref_sys` uden RLS og anonymt læsbar via PostgREST samt PostGIS i `public`. `spatial_ref_sys` må ikke ændres automatisk; ejer skal vælge read-only policy/revokes eller acceptere den dokumenterede undtagelse.

## Næste handlinger

1. Kør `supabase start`, frisk `db reset`, den versionsstyrede pgTAP-suite og DB-lint i en disponibel lokal Docker/Podman-stack; fejlret, indtil replay og alle 105 cases er grønne.
2. Efter særskilt stagingmandat: anvend kun de to forward migrationer på `xdvqdzdpyceojbdknofi`, kør rigtig Auth-båret TUS POST/PATCH/finalize/cancel samt cross-user/cross-tenant/expired/member-revoke-tests, og ryd eksplicitte testfixtures.
3. Åbn derefter staging-Workerens projektupload, verificér en reel FØR-batch inkl. retry i samme åbne wizard og kontrollér at filer uden verificeret GPS/UTC forbliver modtaget men ikke kortklare. UI-pause og reload-resume er særskilt **AFVENTER**.
4. Afgør håndteringen af `public.spatial_ref_sys`; kør derefter security advisor igen. Flyt ikke PostGIS-extensionen uden en særskilt kompatibilitetsmigration.
5. Verificér den betroede orphan-route mod rigtig staging-Storage med cancelled/expired/missing-object, retry, stale-token og parallel worker; vælg derefter eksplicit scheduling og eventuel ledger-purge uden at ændre produktion.
6. Luk det resterende TOCTOU-gap i observations-ingest med databaseatomisk relationvalidering.
7. Verificér autentificeret initial navigation og SSR-/RLS-samspil med en request-scoped serverklient før nye 4DM-domæner.

## Genoptagelseskontrol

Læs dette dokument, backlog, QA-matrix, beslutninger og seneste run-log. Kør derefter `git status --short --branch`, kontrollér at eksisterende ændringer tilhører den aktive cyklus, og overskriv dem ikke. Der må ikke startes P1/P2, commit/push eller ekstern handling uden opfyldt gate og mandat.
